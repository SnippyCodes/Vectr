{
  "buildCommand": "cd frontend/vectr-app && npm install && npm run build",
  "outputDirectory": "frontend/vectr-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
